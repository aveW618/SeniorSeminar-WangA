# SeniorSeminar
A student project for Managing Scheduling and Optimizing a Solution that reduces conflicts

## Develop a Java Based Solution to Minimize Scheduling Conflicts

The attached CSV/Spreadsheet file is the result of sending out a google form and having students enter their top 5 ranked choices, using the mini table in the spreadsheet that contains the sessions, IDs and speakers.

**Feel free to clean up the spreadsheets, and even create new ones if needed, HOWEVER - you many not alter student preferences in the fiel or spreadsheet.

- 5 Time Slots 
- 5 Classrooms (16 students max/ class)
- Sessions Cannot Run more than Twice
- Notice that some sessions are taught by same person
- Student May Not Repeat a Session
- Schedule a Student for 5 Session

**Goal is to design and code an algorithm that generates a schedule for the students, given structure above, with the fewest number of conflicts

- Generate a schedule for the sessions and speakers, 
- Create lists for each student so they know what sessions to attend, rooms and times.


Update for the weekend of 2/21-2/22 (missed class, makeup):
I worked on fixing some syntax errors and also deleting redundant code for my counter class, instead choosing to use loops to keep track on info in arrays. I used counters to keep track of popularity for each of the instructor sessions to help determine which ones should have 1 or 2 sessions running. I also reorganized some of the timeslot and session room info into 5 x 5 arrays to better place and keep track of students and their locations for the day (this was in the loading and assignment array I created to help assign students). I started my main class that helps overall organization and keeping track of info from all 5  other classes to merge in the end when I assign students to their individual classes. I also refined my oragnization of Instructor info by starting to work on a list to keep track of them and the sessions they are teaching (still in progress). My next steps would be to refine any syntax errors and finish loading Instructor info as well as checking to make sure they aren't teaching overlapping sessions. I also need to rethink through my logic on how my students will be assigned to sessions now that a lot of my foundational structures are finished.

Update for February 23 (in-class): 
I talked a bit with Mr. Twyford about my next steps for the project which will now include loading the Student objects into the 5 x 5 array (prioritizing timeslots because there are the highest risks of conflicts there in comparison to the rooms). I also talked about more of my logic (ranking by popularity) and some of the advantages/disadvanategs of ranking my timestamps. I realized I hadn't actually dedicated a specific class and code to my Student object so I created that using the same coding basis as my instructor object clas. I also commented a bit more about my code. My next steps are to load the student objects and also to merge all of my components (instructor objects, student objects, 5 x 5 arrays, counter arrays, etc.) together to actually apply them in my system that will assign the students to their respective classes. I struggled a lot with compiling errors, which I was then able to fix outside of class, as well as thinking through my logic better with respect to what info is actually necessary in creating Student, Session, and Instructor Objects and what isn't. For example, I ended up removing session name info as the session ID would be easier and more efficient for the computer to keep track of than the long sesison names. I also refined my loading methods employing the add method for arraylists and also creating a main method for each class to help test if the code runs successfully and loads the students and sessions. 

Update for March 12 (in-class):
Worked on the next processes for loading the actual schedules themselves through popularity by improving my Counter class which keeps track of popularity. I wanted to create the 2nd session of a course if it so popular that the 1st session is filled. I still need to work on syntax and changing the errors but my logic is there. I plan on adding 100 to the session ID so that when I mod the session1 and session2 values, I will still get the same session but just different versions. I started the class by working on creating my 5 x 5 timetable for my rooms and timeslot using an array list and was going to loop through the rows and the sessions to find the instructor information and compare them between sessions in the same row/ same timeslot to make sure that an instructor isn't teaching two sessions at the same time. When I talked with Mr. Twyford about some of the logic/syntax/code/structure of this section, he recommended that I first work on laying down a schedule as a basis and later consider these details Thus I commented the code I had begun to write and shifted to my counter class to improve my popularity trackers.

Update for March 13 (in-class, shortened class): 
Added some more work to my Counter class in order to progress towards creating an array list that has the popularity ranking of the sessions. Did this by creating some array lists (first to keep track of the sessions and then to keep track of the ranked sessions). I also created a counter to keep track of the number of sessions (numSid). I'm also in the process of creating a loop that compares every value in the array lists to determine which sessionID count values (which counts attendees) are greatest and are thus at the beginning of the popularity list and those that are smallest and at the end of the popularity list. I still need to change the loop though because right now it only compares neighboring values and not the values of the entire list. I also need to fix syntax errrors and begin working on debugging because most of my code is just a part of my thought process but not completly accurate yet. I also worked a little on some editing for my work in Counter that helps me create the schedule. This will be my main focus next class so that I can actually print out a basic schedule, even though it might not yet account for all the complexities of the program itself. My ppopularity coutner will help me create my schedule, because I plan to place the sessions based on popularity into the timeslots.

Update for April 1 (in-class):
Debugging my code, needed to create two load methods for my Instructor class which I didn't realize before. Fixed it so it now compiles successfully. I need to finish my counter to load the second choices if the first choice sessions of students are filled. Then I can move onto loading my schedules in my main class. I plan on finishing my for loop to loop through all the students and their first and second choices and add if loops the session a student desires as their first choice is filled. Also need to account for if the second choice is filled (would need to go to the third session ranking). I also plan on using a Math.random() line of code for the 4-5th choices of students depending on whether or not they were able to get into all of their classes. I'm not quite sure if this would be in the counter class or the main class yet. I also did to finish debugging my loadingAndAssignments class.
